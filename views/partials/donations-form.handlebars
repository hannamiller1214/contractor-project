<!-- views/partials/donations-form.handlebars -->

<div class="form-group">
  <label for="title">Title</label>
  <input name="title" class="form-control" placeholder="Pickup Basketball" value="{{donation.title}}" />
</div>
<div class="form-group">
  <label for="desc">Amount</label>
  <textarea name="desc" class="form-control" placeholder="Amount of your donation">{{donation.desc}}</textarea>
</div>
